import { BulletsDirective } from './bullets.directive';

describe('BulletsDirective', () => {
  it('should create an instance', () => {
    const directive = new BulletsDirective();
    expect(directive).toBeTruthy();
  });
});
