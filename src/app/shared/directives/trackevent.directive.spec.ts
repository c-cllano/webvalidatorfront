import { TrackeventDirective } from './trackevent.directive';

describe('TrackeventDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackeventDirective();
    expect(directive).toBeTruthy();
  });
});
