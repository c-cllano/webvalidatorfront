apiVersion: apps/v1
kind: Deployment
metadata:
  name: uitemplate
  namespace: pre-microservice
spec:
  replicas: 5
  selector:
    matchLabels:
      app: uitemplate
  template:
    metadata:
      labels:
        app: uitemplate
    spec:
      nodeSelector:
        kubernetes.io/os: linux
        kubernetes.io/arch: amd64
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - uitemplate
              topologyKey: "kubernetes.io/hostname"
      containers:
        - name: uitemplate
          image: #{ACR_NAME}#.azurecr.io/nextgenid.api.uitemplate:v.#{Build.BuildId}#
          resources:
              requests:
                cpu: 25m
                memory: 128Mi
                ephemeral-storage: 2000Mi
              limits:
                cpu: 300m
                memory: 500Mi
                ephemeral-storage: 5000Mi
          ports:
            - containerPort: 7288
          envFrom:
            - configMapRef:
                name: uitemplate-config
          env:
            - name: ASPNETCORE_URLS
              value: http://+:7288
            - name: TARGET_PLATFORMS
              value: linux/amd64,linux/arm64
